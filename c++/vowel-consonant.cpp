#include<iostream>
#include<conio.h>
using namespace std;

int main(){
    char ch;
    cout<<"Enter a character: ";
    cin>>ch;
    if ( ch=='a' ){
        cout<<ch<<" is a vowel"<<endl;

    } else if (ch == 'e'){
        cout<<ch<<" is a vowel"<<endl;

    } else if (ch == 'i'){
        cout<<ch<<" is a vowel"<<endl;

    } else if (ch == 'o'){
        cout<<ch<<" is a vowel"<<endl;

    } else if (ch == 'u'){
        cout<<ch<<" is a vowel"<<endl;

    } else {
        cout<<ch<<" is a consonant"<<endl;
    }




    return 0;
}